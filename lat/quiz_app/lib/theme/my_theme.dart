import 'package:flutter/material.dart';

final kColorScheme =
    ColorScheme.fromSeed(seedColor: const Color.fromARGB(187, 233, 79, 128));

final myTheme = ThemeData().copyWith(
  colorScheme: kColorScheme,
);
